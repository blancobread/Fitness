import React from 'react';
import {Typography} from "@mui/material"

const Navbar = () =>{
return(
    <header>
        <Typography>Hello {"insert name here"}</Typography>
    </header>
)
}

export default Navbar